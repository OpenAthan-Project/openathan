# GitHub Actions

CI/release workflows will be added once the initial firmware build is reproducible. Avoid adding automation that publishes firmware before hardware validation and release signing/versioning decisions are made.
