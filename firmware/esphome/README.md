# ESPHome Firmware

ESPHome is the preferred initial embedded framework for OpenAthan.

This directory will contain the build configuration and OpenAthan-specific external components. End users should not be expected to edit YAML manually; stable releases should be distributed as precompiled firmware through the browser installer.
