# OpenAthan ESPHome Component

Planned adapter between ESPHome and `openathan-core`.

Do not put prayer calculation policy exclusively in ESPHome YAML. Core behavior should remain testable outside ESPHome.
