#pragma once
// JSON transport is compiled by the firmware builds; adapter tests exercise
// the same typed settings entry point without substituting a JSON parser.
class JsonObject;
class JsonObjectConst;
